#ifndef KINETIC_NOTATION_TYPES_H
#define KINETIC_NOTATION_TYPES_H

// std
#include <stdbool.h>
#include <stddef.h>

typedef struct {
  size_t major;
  size_t minor;
  size_t patch;
} kn_version;

#endif // KINETIC_NOTATION_TYPES_H
