#include "api_internal.h"

// std
#include <stdlib.h>

Structure *structure_create() { return NULL; }

void structure_add_key(Structure *structure, const char *key_name,
                       key_type key_type) {}
